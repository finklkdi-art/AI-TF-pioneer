# BLUE NINE — Backend only deps (Render 배포용, slim).
# 루트 requirements.txt 는 로컬 개발용 (Streamlit 등 포함).

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2
python-dotenv==1.0.1            # 로컬 .env 자동 로드 (운영선 noop)

# ── LLM 통합 (Source 4 Precise 모드 — PDF + xlsx 시맨틱 파싱)
llama-parse==0.6.94             # PDF → markdown (버전 핀 — 재배포 안정성)
anthropic==0.102.0              # Claude API SDK — markdown → 구조화 라인아이템
tabulate==0.9.0                 # DataFrame.to_markdown 의존성 (시맨틱 파서)

# 데이터 처리
pandas==2.2.3
numpy==2.1.2
openpyxl==3.1.5
xlrd==2.0.1                 # 구형 .xls (Billing Status.xls) 지원
pdfplumber==0.11.4
pypdf==5.0.1
